server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SuryaPranesh-dev/config-repo.git
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: refresh
